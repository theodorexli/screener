# Copy relevant lines into a root `.env` file for local overrides.
# For most local work you can leave these unset and use the Vite proxy.

# --- Production build / CI ---
# VITE_WORKER_URL=https://screener-api.YOUR_SUBDOMAIN.workers.dev
# VITE_PREDICTIONS_WORKER_URL=https://screener-predictions.YOUR_SUBDOMAIN.workers.dev
# VITE_PUBLIC_POSTHOG_KEY=phc_your_posthog_project_key
# VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# --- Vite dev proxy overrides (optional) ---
# Default proxies:
#   /api            → http://127.0.0.1:8787
#   /api/predictions → http://127.0.0.1:8789
# WORKER_PROXY_TARGET=https://your-api.workers.dev
# PREDICTIONS_PROXY_TARGET=https://your-predictions.workers.dev

# --- Cloudflare Worker secrets (set with wrangler, not in frontend .env) ---
# ALPACA_API_KEY
# ALPACA_API_SECRET
# NOTION_API_KEY
# NOTION_DATABASE_ID
# GEMINI_API_KEY
# LOGO_DEV_PUBLISHABLE_KEY
# ALLOWED_ORIGINS   # comma-separated frontend origins
